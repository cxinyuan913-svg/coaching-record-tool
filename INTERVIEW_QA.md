# 面試攻防問答集

用詞規則跟 `DEVLOG.md` 一致：「AI」指程式/Claude 做的事，「我」指你自己
做的決定與行為。每題答案控制在口頭 30 秒到 1 分半可以講完的長度。標註
**〔此題需你補充〕** 的地方，代表答案牽涉到我不知道的個人背景或你還沒
表態的立場，內容是我先搭好的框架，細節要你自己填。

---

## 1. 專案動機與需求層面

### Q1. 為什麼做這個專案、解決什麼問題？

我自己在教羽球，一直用手動方式（心算、記事本）算學費、記誰付了誰沒付、
記套組還剩幾堂。學生一多之後很容易記錯、漏收，尤其是套組課──學生一次
繳一筆錢買 8 堂，中間有人請假、有人臨時想加時間，光靠記憶跟記事本完全
不夠用。這個工具就是把我原本腦子裡在做的那套帳，變成系統自動幫我算，
同時因為我在轉職，也把它做成一個可以展示我怎麼跟 AI 協作開發的作品。

### Q2. 為什麼不用現成的排課/記帳軟體？

現成的記帳軟體大多是給一般小店用的通用收支記錄，排課軟體則大多只處理
「時間表」，兩者沒有一個能處理我這個領域裡真正麻煩的地方：套組是「一次
收錢、分次消耗」，教練費跟場地費要分開算（場地費只是代收代付、不是我的
收入），還有請假要自動順延、額度用完要擋下超收。這些是我這個特定使用
情境的規則，現成工具不會為了我一個人客製這些邏輯。
**〔此題需你補充〕**：如果面試官追問「你具體比較過哪幾套工具」，這部分
要你自己補充實際考慮過的名字（例如是否用過某個記帳 App 或 Google 表單
記帳），我沒有這個資訊。

### Q3. 使用者只有你一個人，為什麼值得做？

第一，這是我每天真的在用的工具，不是為了面試臨時做的 demo，資料是我
真實的排課紀錄，這代表它必須真的堪用，不能只是「看起來能跑」。第二，
單人使用不代表設計簡單──套組計費、請假順延、額度管理這些規則的複雜度
跟使用者數量無關，反而因為是我自己在用，我對每個功能是不是「真的好用」
的標準更嚴格，這正是我想證明的：我能把一個真實、我自己會持續用下去的
工具做出來，而不是寫一個交差了事的練習題。

---

## 2. 資料模型與設計決策層面

### Q4. 為什麼這樣設計資料表，各表的職責？

六張表：`students`（學生基本資料跟等級）、`venues`（場地跟訂場規則）、
`price_rules`（單堂制的價目表，用人數區間＋等級查價）、`packages`（多堂
套組的合約主體：總堂數、費率、狀態）、`lessons`（每一堂實際課程，是整個
系統的核心，套組課跟單堂課都在這張表，用 `package_id` 是否為 NULL 區
分）、`adjustments`（額外費用/差額，跟課程綁定但獨立記錄跟結清）。這樣
分的原則是：`lessons` 永遠是「實際發生的一堂課」這個唯一事實來源，其他
表則是圍繞著它的合約、規則、跟例外狀況。

### Q5-1. 多堂包用「攤提」而非「購買時一次計收入」，為什麼？

套組是學生一次付清 8 堂的錢，但如果我在購買那一刻就把整筆錢算成當月
收入，收入統計會很失真──某個月可能因為剛好賣掉一個大套組，數字暴衝，
但那些課其實要上好幾個月。我的做法是每一堂課各自記一個 `revenue_amount`
（依費率跟該堂實際時長算），月/年收入統計是依「課程實際發生的日期」去
加總，這樣才能反映我每個月實際「教了多少」，而不是「哪個月收了多少現
金」，兩者在會計概念上其實是「認列時點」的差異，我選的是比較貼近實際
服務發生時間的算法。

### Q5-2. 請假順延會讓某個套組的 lessons 筆數超過原本的堂數，這是設計還是 bug？

這是設計。套組的「總堂數」（`total_sessions`）代表的是「學生付了幾堂
錢」，不等於「這個套組底下有幾筆 lessons 紀錄」。請假時，原本那一堂會
留著（狀態改成請假、`deduct_session` 設成不扣、金額歸零，當作歷史紀
錄），系統另外補一筆新的課程去遞補，所以底下的 lessons 筆數可能是 9
筆、10 筆，但「真正算數、會扣額度」的堂數還是鎖定在 8。這樣設計是為了
同時保留「這堂原本排定過、後來請假了」的歷史軌跡，跟「學生總共要上滿
8 堂」這個契約約束，兩者不衝突。

### Q5-3. `bookings` 表原本獨立、後來併回 `lessons`？

**這題的前提我要先更正**：我實際查過完整的 git 歷史紀錄，這個專案從
第一個建表的 commit 開始，訂場相關的欄位（`booking_status`、
`booked_at`）就一直是 `lessons` 表上的欄位，從來沒有存在過一張獨立的
`bookings` 表、也沒有經歷過「先分開、後來又合併」的重構過程。**這題如
果你手上的資訊來源跟我查到的不一樣，麻煩先跟我確認一次**，不然照這個
題目的前提去回答，會變成講一段沒真的發生過的開發故事，被追問細節時
會兜不攏。如果你想討論的其實是「為什麼一開始就決定不要獨立成一張表」
這個假設性的設計考量，我可以另外幫你寫，但那是「設計時的考量」，不是
「做了又改回去」的實際歷史。

### Q5-4. `price_rules` 用人數區間而不是固定人數，為什麼？

因為人數的定價邏輯本質上是「級距」而不是「精確匹配」──1 人一個價、2
人一個價、3 人以上（不管是 3 人、5 人、10 人）都是同一個價，如果每個
人數都存一筆固定值的規則，規則會一直長、還要考慮「11 人怎麼辦」這種
沒人設定過的邊界。用 `headcount_min`/`headcount_max` 區間，3 人以上這
種「無上限」的情境用一個很大的數字（例如 999）當上限就能表示，規則
數量少、好維護，也不用擔心漏掉某個特定人數沒有對應規則。

### Q5-5. `adjustments` 為什麼獨立成一張表，不做成 `lessons` 的欄位？

`adjustments` 處理的是不規則的例外收費──人數臨時變動的差額、臨時改
場地的加價、其他雜項──這些不是每堂課都會有，而且同一堂課可能同時有
好幾筆（例如又改場地、人數又變動）。如果做成 `lessons` 的欄位，一堂
課最多只能記一筆例外，而且沒辦法個別追蹤「這筆有沒有結清」。獨立成表
之後，一筆額外費用有自己的結清狀態跟結清時間，可以在未收款清單裡統一
列出所有還沒結清的項目，不用回頭一堂一堂找。

### Q6. 哪些設計你事後覺得可以更好？

有兩個地方我覺得目前是不完整的。第一，`lessons` 的狀態有一個「取消」，
但規格原本設計成請假／取消要有一個「要不要扣堂數」的手動選項讓我自己
決定，實際上這個選項從來沒有真的做到畫面上，取消目前的行為是寫死的，
不夠彈性。第二，套組狀態裡有一個「已過期」（expired），但整個系統從
頭到尾沒有任何地方會真的把套組設成這個狀態，等於是一個放在那邊沒用到
的欄位。這兩個都是我可以在下一階段補上的技術債。

---

## 3. 技術選型層面

### Q7. 為什麼選 FastAPI + 原生 JS，不是 React？

這是我在專案一開始的規格文件裡就寫下來的決定：時程有限的情況下，我希望
把心力優先放在「領域邏輯對不對」（套組計價、請假順延這些商業規則），
而不是同時去學一個新框架、搞 build 工具鏈。原生 JS 對這個規模（8 個頁
面、沒有複雜的跨元件狀態）完全夠用，也讓部署變得單純——就是靜態檔案，
不用打包。FastAPI 則是因為它跟 Python 的型別系統搭配得好、文件自動產
生，很適合我一邊寫一邊釐清資料格式。

### Q8. 為什麼用 SQLite？之後怎麼遷移？

SQLite 對單人、單機使用的工具來說完全夠用，不用另外裝資料庫服務、備份
就是複製一個檔案，開發起來最單純。如果之後要遷移（例如要上雲端讓我
在手機上也能用），SQLAlchemy 本身是資料庫無關的，主要工作是把連線字
串換成 Postgres，重新跑一次 migration，商業邏輯層完全不用動。真正需要
留意的是 SQLite 允許的一些「隨便」（例如型別檢查比較鬆），遷移前要跑
過一次資料驗證，確保沒有不合規的資料混進去。

### Q9. 如果要支援多位教練使用，架構要怎麼改？

現在的資料表完全沒有「這筆資料屬於哪個教練」這個概念，六張表全部要加
一個 `coach_id`，而且所有查詢都要補上這個過濾條件，這是最大工程量的
部分。另外現在完全沒有登入機制，要加使用者驗證（帳號密碼或第三方登
入）、每個 API 都要檢查「這個教練只能看到自己的資料」。資料庫也建議
從 SQLite 換成正式的伺服器型資料庫，因為要處理多人同時寫入。

---

## 4. AI 協作與驗證層面

### Q10. 這個專案有多少是 AI 寫的？

老實說，程式碼幾乎全部是 AI 寫的。但我的工作不是「轉述需求給 AI 打
字」，而是：定義規格、看 AI 做出來的東西符不符合我要的、拿真實資料
去操作找出不對的地方、決定怎麼修正、要求 AI 用什麼方式驗證。這個角色
比較接近工程團隊裡的 PM 兼 QA，不是單純的打字員——差別在於我對這個
系統的商業邏輯有完整的理解，可以精準指出「這裡不對」跟「應該要這樣」。

### Q11. 你怎麼確保 AI 的實作符合你的意圖？

三層：第一，我會要求 AI 在功能做完、給我看之前，先自己在瀏覽器裡把
流程走一遍（見 `DEVLOG.md` 案例 2）。第二，我自己一定會拿真實排課資
料實際操作過一次，不是看程式碼掃過去就算了——這次專案裡好幾個 bug
（例如剩餘堂數沒扣、請假順延邏輯不對）都是我實際用了之後才發現的。
第三，遇到金額、堂數這種數字類的邏輯，我會直接盯著畫面上的數字去對，
數字對不上馬上要求解釋原因。

### Q12. 有沒有 AI 做錯、你發現並修正的例子？

最經典的是請假順延邏輯：我說「請假後面全部順延一週」，AI 照字面做成
整批往後平移，我測試後發現不對，我要的其實是「只在最後面補一堂」，
兩個版本間隔 6 分鐘就改掉了。這個例子剛好說明：**問題不一定是 AI 做
錯，常常是我自己描述的需求本身有歧義**，AI 是照我說的做，我的責任
是驗證出來、講清楚我真正要的是什麼。詳細案例都寫在 `DEVLOG.md` 裡。

### Q13. 你用了哪些驗證方式？

三種：第一，真實資料手動操作——每個功能做完我都會拿我自己真實的排課
資料實際點過一次。第二，瀏覽器自動化——我要求 AI 用一個 Chrome 自動
化工具實際跑過表單填寫、點擊、截圖，甚至直接讀取畫面元素套用的 CSS
數值，這個方法抓到好幾個「畫面看起來正常、其實底層出錯」的 bug（時
區偏移、CSS 優先權蓋掉顏色設定）。第三，API 層級的資料驗證——用
`curl` 直接呼叫後端 API、核對資料庫筆數，確認刪除、新增這類操作真的
如預期影響到資料庫。
**〔此題需你補充〕**：如果任務三的 pytest 測試在你面試前有實際完成、
你也看過測試內容確認符合你的意圖，可以在這題補一句「另外我也請 AI
針對核心規則寫了一組自動化測試（pytest），涵蓋批次排課、請假順延、
價目計算、人數差額、收入統計這幾條規則」；如果還沒做完，這句先不要講。

### Q14. 如果沒有 AI，你自己寫得出來嗎？

**〔此題需你補充〕**：這題完全取決於你自己的程式能力背景，我沒有這個
資訊，不能幫你編。建議誠實回答：如果你原本就有一定的程式基礎，可以說
「邏輯我完全看得懂、也能自己寫，只是會花比較久時間，AI 讓我把時間花
在打磨商業邏輯而不是重複的 CRUD 樣板上」；如果你是這個專案才開始比較
深入寫程式，建議誠實說「這是我學習跟 AI 協作開發的過程，但我完全理解
系統在做什麼、為什麼這樣設計」，並且用你在這個文件裡展現出的判斷力
（抓到需求歧義、抓到 bug）去佐證你不是只會轉述。**千萬不要編造一個
你答不出細節的說法**，被追問寫程式細節答不出來風險更大。

### Q15. 你認為 AI 協作開發的風險是什麼？

第一，AI 會非常忠實地照字面實作，如果我自己講的需求有歧義，AI 不會
主動幫我釐清，會直接做出一個「技術上對、但不是我要的」版本，這次專案
裡就發生過。第二，AI 產生的程式碼「看起來對」的比例很高，如果我沒有
拿真實資料實際操作、只是看過程式碼掃過去，很多瀏覽器渲染層級的問題
（時區、CSS 優先權）根本不會被發現。第三，如果我對系統的商業邏輯沒有
自己的判斷，很容易變成單純「AI 說了算」，這樣就沒辦法對做出來的東西
負責。這些風險都要靠「我自己保留最終判斷、堅持用真實資料驗證」去對沖。

---

## 5. 工程實務層面

### Q16. 版本控制怎麼做的、commit 習慣？

每完成一個小功能就 commit 一次，commit 訊息用繁體中文描述具體做了什麼
（不是「fix bug」這種空泛訊息），例如「修正套組剩餘堂數：日期已過的
課程要自動算已用掉」。這個習慣讓我事後可以直接用 `git log` 重建整個
開發過程的時間線，這次面試準備的 `DEVLOG.md` 就是直接從 commit 紀錄
整理出來的，每一項都能對到一個實際的 commit。

### Q17. 測試涵蓋了什麼、沒涵蓋什麼、為什麼？

涵蓋的部分：所有功能都經過真實資料的手動操作驗證，關鍵互動流程（新增
套組、刪除確認、額度扣款）都跑過瀏覽器自動化驗證。沒涵蓋的部分：目前
沒有一組長期保留、每次改動都自動重跑的回歸測試（regression test），
每次驗證都是「當下手動跑過一次」，沒有寫成程式碼保留下來。這是因為
開發前期功能變動很快，先以「驗證這次改動有沒有效」為優先；等核心規則
穩定之後（就是這幾天在做的），才回頭把最重要的業務規則寫成自動化測試。

### Q18. 遇到最難的技術問題是什麼？怎麼解決？

最棘手的是一個「畫面上看起來設定對了、實際卻沒生效」的 CSS 問題：我
要求行事曆週檢視的顏色改淺一點，改了之後文字顏色卻還是白色，肉眼完全
看不出原因。後來是直接用瀏覽器工具讀取畫面元素「實際套用」的樣式數值
（不是看我寫的 CSS 檔案），才發現 FullCalendar 內部有一個優先權更高
的規則蓋掉了我設定的文字顏色，必須改成直接指定內層元素的顏色才會生
效。這個經驗讓我確認：**遇到「邏輯上應該對、但畫面不對」的狀況，一定
要去看瀏覽器實際計算出來的結果，不能只憑讀程式碼猜**。

### Q19. 如果專案要交接給別人，你會怎麼做？

其實這個專案已經具備基本的交接文件：`CLAUDE.md` 記錄開發慣例跟技術棧
決策，`spec/` 資料夾拆成核心規格、找空檔排班 Agent、場館優化建議三個
子系統各自的規格文件，加上這次新增的 `DEVLOG.md` 記錄了每個設計決策
背後的原因跟踩過的坑。如果要正式交接，我會再補上兩塊：一份 README
講怎麼把環境架起來、一組涵蓋核心業務規則的自動化測試，讓接手的人改
程式碼時能立刻知道有沒有把某條規則改壞。

---

## 6. 延伸與規劃層面

### Q20. 接下來要做什麼、為什麼還沒做？

短期最優先的是把套組「請假／取消要不要扣堂數」補成一個真正可以手動
選擇的選項（目前是寫死的邏輯，前面提到的技術債）。中期規劃在 `spec/`
資料夾裡已經寫好兩份規格但還沒動工：一個是「找空檔排班 Agent」，另一
個是「場館優化建議」。還沒做的原因是這兩個都需要先有穩定的核心資料
（真實排課紀錄累積到一定量）才有意義去驗證演算法對不對，我優先把地基
（套組、收費、行事曆）做穩，再往這兩個進階功能推進。雲端部署也是刻意
延後的決定，先確保本機版本好用、資料結構穩定，再考慮上雲。

### Q21. 找空檔排班 Agent 的設計思路：為什麼 LLM 只負責解析與產出？

這個功能的情境是：學生說「下週一到五，台北都可以」，系統要在我既有的
行事曆裡找出最省交通時間的空檔。我在規格裡明確把它拆成兩塊：LLM 只
負責「把學生的自然語言需求，轉成結構化的日期範圍跟地區限制」，還有
「把最終算出來的候選時段組成一段文字訊息」；中間真正「哪些時段可以
用、交通時間怎麼算、要排除哪些衝突」這些運算，全部用一般的、確定性的
程式邏輯處理，不讓 LLM 去猜。理由很直接：找空檔是一個有明確對錯答案
的組合問題，LLM 用機率的方式推理，可能會算錯、也沒辦法保證每次結果
一致；把它交給一般程式，結果才會是可預期、可驗證的，LLM 只做它真正
擅長的「理解語言」跟「組織成人看得懂的文字」這兩件事。

### Q22. 場館優化建議為什麼是「建議」而非自動執行？

因為系統要調動的是已經跟學生約好的課程時間或場地，這個約定是我跟學生
之間的關係，系統不能自己決定「幫你改掉」。就算演算法算出來的組合在
邏輯上更省交通時間，實際上學生可能剛好那天有事、或不喜歡臨時被換場
地，這是只有我（或我親自去問學生）才能確認的事。所以我把它設計成
「系統只給建議、我確認要採用之後，那堂課會被標成『場館待確認』的狀
態，提醒我要親自跟學生確認過、學生答應了才算數」，這是一個刻意保留
人在迴圈裡做最終決定的設計，不是技術做不到自動執行。

---

## 7. 壓力測試型問題

### Q23. 這個專案的技術含量會不會太低？

如果只看「六張表 CRUD」，確實看起來很基礎。但這個專案的難度不在
CRUD，在於商業邏輯的正確性——套組請假要不要扣堂數、扣了之後要不要
自動補課、補課會不會跟其他課衝突、金額要用哪個時間點的費率去算、
統計要不要排除取消的課，這些規則彼此牽動，稍微沒想清楚就會讓帳算
錯。我在這個專案裡花最多力氣的，正是把這些規則定義清楚並且驗證正確，
而不是把畫面刻出來。

### Q24. 這些邏輯用試算表也能做，為什麼要寫程式？

試算表可以記帳，但沒辦法「自動幫我做決定」——請假自動找出最後一堂
的下一週補課、額度用完自動擋下超收、日期一過自動更新剩餘堂數，這些
都是「系統主動幫我算跟擋」，試算表只能被動記錄我自己算完的結果。另
外試算表在「一筆資料牽動多筆資料」（例如刪掉一堂課要重新分攤整個
套組的金額）這種關聯性計算上，公式會變得極度脆弱、容易改壞，用真正
的關聯式資料庫跟程式邏輯處理才穩定可靠。

### Q25. 你說是你設計的，但實作都是 AI 做的，你的貢獻是什麼？

我的貢獻是這整份問答集裡反覆出現的東西：**定義規則、驗證結果、發現
落差、做出取捨**。具體例子——套組計價要從「設一個總價」改成「分別
設堂課費跟場地費、用時數乘出來」，這是我主動要求的商業邏輯翻轉，不
是 AI 自己想到的；請假順延邏輯做錯了，是我測試發現、講清楚我要的
行為，AI 才改對；收入統計會不會把取消的課算進去，是我要求 AI 做開放
式健檢才挖出來的風險。這些都是需要對這個系統的商業邏輯有完整理解才
做得到的判斷，不是「AI 寫、我簽名」。

### Q26. 你完全沒有商業開發經驗，怎麼證明你能勝任？

**〔此題需你補充〕**：這題牽涉到你實際的職涯背景、應徵的職位類型，我
完全沒有這些資訊，沒辦法幫你寫。我能提供的框架是：這份文件加上
`DEVLOG.md`，本身就是在回答「我怎麼跟 AI 協作把一個真實需求做成一個
真的在用的系統」，如果應徵的職位重視的是「需求釐清、驗證、跟 AI 協
作」這類能力，這個專案就是直接的證明；但如果面試官要問的是「你有沒
有在團隊裡跟其他工程師協作、走過 code review、上線流程」這類經驗，
這題需要你自己誠實補上你有的、沒有的部分是什麼，不要用這個個人專案
去冒充團隊經驗。
