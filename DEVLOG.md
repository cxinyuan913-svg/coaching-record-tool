# 開發歷程重點整理

給面試用的版本。**用詞規則：本文一律用「AI」指稱程式/Claude 做的事，用
「我」只指使用者（你）自己做的事，兩者絕不共用「自己」這種模糊代名詞**，
避免面試時分不清楚是你做的還是 AI 做的。分量不平均是刻意的：面試最值得
講的案例寫詳細，其他的只列一行帶過。技術細節（欄位名、commit 編號）都
收在表格最右欄，講的時候不用背，臨時要查證再看就好。

## 一句話總結

- 這是一套幫羽球教練記帳、排課的工具，資料是我自己真實在用的排課紀錄。
- 開發方式是我跟 AI 一起做：我描述需求、AI 寫程式，**但每個功能都是我
  自己拿真實資料實際操作過，抓到不對的地方再要求 AI 修正**，不是拿到
  就收。
- 我也會主動要求 AI 在交付給我看之前，先自己跑過一輪測試，或主動做開放式
  的程式碼健檢——這是我設計的工作方式，不是 AI 自發的，過程中因為這套
  流程，好幾個問題在我看到之前就先被擋掉了。

---

## ★ 面試重點案例（挑 4 個最值得展開講的）

### 1. 我自己講的需求有歧義，AI 做出來我才發現不是我要的

我一開始跟 AI 說：「有人當週請假的話，當週以後的課程全部順延一週」。

字面上聽起來就是「後面全部往後平移一週」，AI 也照這個字面意思做了。結果
我實際點開行事曆一看：**後面八堂課全部被往後移了一週，不是我要的**。我
真正想要的是「只在最後面多補一堂課」，中間其他課完全不動。

我馬上跟 AI 反映、AI 改掉——前後兩個版本間隔只有 6 分鐘，看得出是「AI
做完、我馬上測、我一發現不對馬上要求改」的節奏，不是隔了很久才發現。

**這個案例證明：** 需求本身用講的容易模糊，一定要做出來、我自己實際操作
過才知道對不對，光靠嘴巴溝通會漏掉很多細節，而我的角色就是那個負責把
關、驗證的人。

### 2. 我要求 AI 交付前要自己先測過一輪，這個習慣讓一個時區 bug 在我看到之前就被擋掉

我要求 AI「排課時間要能一次帶入一整批日期」（例如每 7 天一堂、共 8
堂）。我平常的要求是：功能做完不能直接給我看，AI 要先自己在瀏覽器裡
把流程走一遍。

**AI 自己測試這個功能的時候**（不是我操作、我當時根本還沒看到這個功能）
發現：明明輸入 10/6，系統跑出來的第一天卻是 10/5，整整少一天。原因是
系統內部用一個現成的日期轉換方式，原理是「先轉換成標準時區再擷取日
期」，但台灣跟標準時區差 8 小時，半夜轉換的時候剛好會被推到前一天。這
種問題只有跨過午夜附近的時間點才會踩到，多數時間（例如中午）測試都不
會發現。

**這個案例證明：** 我懂得設計「AI 自己先驗證過一輪，我看到的已經是測試
過的版本」這種工作方式，而不是每次都靠我自己一個個點過去試——這樣可以
把顯而易見的問題先過濾掉，我可以把心力留在判斷「這個行為到底符不符合
我要的」這種需要人判斷的事情上。

### 3. 我請 AI 主動做開放式健檢，AI 自己揪出一個還沒發生的金額計算 bug

有一次我請 AI「自己想一下還有什麼可以優化」——這是一個開放式的指令，
我沒有指定要查哪裡。

**AI 自己去檢查了收入統計的計算邏輯**（這不是我指定要查的地方，是 AI
自己判斷這裡可能有風險），發現一個還沒發生、但邏輯上遲早會出錯的問題：
如果一堂已經收費的課後來被標記「取消」，系統的月收入、年收入統計不會
把這筆錢扣掉，等於取消的課還是被算成收入。因為我目前的真實資料裡還
沒有任何一堂被取消過，這個問題還沒有真的影響過我看到的數字。AI 修好
之後自己寫了一組測試步驟驗證（建一筆假的已收費課程確認金額增加、改成
取消確認金額消失），我確認結果沒問題後才讓它刪掉測試資料。

**這個案例證明：** 我懂得怎麼給 AI 開放式的稽核任務去主動發掘我自己都
還沒想到的風險，並且會要求它拿出驗證證據，而不是只等我發現問題才提出
需求。

### 4. 同一個排課介面，因為我提出的真實需求，重做了三次

- **第一版**：套組建立時只要填「第一堂日期」跟「固定星期幾」，系統自動
  排好連續幾週的課。做起來最簡單，但完全沒辦法跳過連假——遇到連假只能
  之後再手動改。
- **第二版**：因為我反映連假問題，AI 改成手動一天一天輸入日期，可以
  自由跳過任何一週。但我實際用了之後，操作上還是要自己對照月曆算日
  期、一天一天打，不夠直覺，我又提出了這個回饋。
- **第三版（目前版本）**：AI 改成直接在畫面上放一個小月曆，我用滑鼠點
  格子就能選日期，點兩次取消，還可以跨月份選；另外保留一個「快速帶入」
  的捷徑（填起始日、間隔天數、堂數，一次產生一整批），但產生完之後我
  還是可以再手動點掉某一天，跳過連假。

**這個案例證明：** 一個功能不會一次到位，是靠我「實際使用、發現卡點、
提出回饋」跑三輪，AI 每輪調整，才收斂成現在好用的樣子。

---

## 其他 bug 修正一覽

| 問題（一句話） | 誰發現的 | 怎麼修的 | 技術細節（commit） |
|---|---|---|---|
| 同一次操作裡，程式還沒真的存檔就先去檢查資料，導致算出舊的結果 | AI 開發早期自己抓到 | 改成每次查詢前先確保資料真的寫進去 | `autoflush=True`，commit `af92101` |
| 刪掉套組裡一堂課後，剩下的課金額沒有重新分攤 | AI 測試時抓到 | 刪除課程時補上「重新分攤金額」這個動作 | commit `3152c92` |
| 請假後又改回正常狀態，原本自動補的那堂課沒有跟著消失 | AI 測試時抓到 | 偵測「從請假改回正常」時，順便清掉多出來的補課 | commit `8d59a33` |
| 套組課程改時長時，畫面上顯示的預估金額用錯了計價方式 | AI 測試時抓到 | 改成套組課程一律用套組自己的費率算，不查一般價目表 | commit `7b0d4e9` |
| 新增套組表單填完按送出完全沒反應，也沒有錯誤訊息 | AI 測試時抓到 | 隱藏的欄位忘記拿掉「必填」標記，導致瀏覽器偷偷擋下送出 | commit `0736563` |
| 套組已經上完的課，剩餘堂數卻沒有減少 | 我回報的 | 改成只要日期已經過去就自動算「用掉」，不用手動標記完成 | commit `59642f9` |
| 套組狀態（進行中/已完成）跟剩餘堂數對不上 | AI 主動健檢抓到 | 讓狀態也改成即時計算，跟堂數保持同步 | commit `fef9dde` |
| 傳給學生的課表訊息，會把已經取消的課也列進去 | AI 主動健檢抓到 | 產生課表時排除取消的課 | commit `fef9dde` |

---

## 其他介面調整一覽

| 原本怎樣 | 卡在哪裡 | 改成怎樣 | 技術細節（commit） |
|---|---|---|---|
| 上課時間可以隨便打文字 | 我實際約課都是整點/半點，容易打錯格式 | 改成下拉選單選時、分，分鐘只有 00/30 | commit `bb5303b`、`c2a2d38`、`dd8fefb` |
| 套組總堂數/時長建好就不能改 | 我排課常有錄入錯誤或臨時異動，完全不能改很不實用 | 開放編輯，會自動連動重算金額 | commit `d7b8a24` |
| 套組是設一個總價、平分到每堂 | 我覺得應該反過來：先算好每小時費率，再乘上時數 | 改成教練費、場地費各自設每小時費率 | commit `1306678` |
| 教練費跟場地費是同一個金額欄位 | 場地費是我代收代付、不是我的收入，混在一起會算錯我的實際收入 | 分成兩個獨立欄位 | commit `dd8fefb` |
| 套組新增只能「當下就先付款、之後排時間」 | 我有些學生是先付整包的錢，但實際上課時間要之後才臨時約 | 新增「先開額度、之後在行事曆逐堂新增」的模式 | commit `0bab5dd` |
| 新增課程時，套組欄位預設是「不屬於套組」 | 我覺得有額度的學生，每次都要手動切換很麻煩 | 只要學生有額度，自動幫他選好 | commit `dd872a3` |
| 未收款清單的「額外費用」只顯示金額跟備註 | 我完全看不出是哪個學生哪堂課，等於沒辦法拿去跟學生對帳 | 補上學生、日期，還可以直接在原地編輯、刪除 | commit `c7c3f93`、`a2bbfc5`、`d51d5ff` |
| 額外費用類型只有人數差額/場地費/其他 | 我覺得臨時改場地是常見的加收原因，但沒有專屬選項 | 新增「臨時改場地」類型 | commit `b38f18b` |
| 訂場提醒清單會一直顯示已經過期的舊課程 | 我反映已經來不及訂的課還一直提醒，浪費注意力 | 過期的課不再顯示 | commit `7d69ec8` |
| 未收款/未結清的按鈕顏色跟其他按鈕一樣 | 我反映沒辦法一眼看出哪些還沒處理 | 改成顯眼的橘色 | commit `0b3eff4` |
| 刪除確認用瀏覽器內建的跳窗 | 我反映樣式醜、跟頁面風格不一致；AI 順便自己檢查發現有一個刪除按鈕根本沒有確認 | 全部換成客製化的置中彈窗，順便補上漏掉的確認 | commit `a91a6b3` |
| 學生列表順序跟建立時間一樣 | 我反映學生一多，找不到目前有在上課的人 | 有進行中套組的排前面 | commit `1014d40` |
| 行事曆課程只顯示開始時間，色塊固定顯示 1 小時長 | 我反映看不出實際上到幾點，週檢視顏色也太淺看不清楚 | 改成顯示完整時段，色塊依實際時長顯示長短，顏色加深；後續我又發現「時間重複顯示兩次」「週檢視顏色太搶眼」「文字顏色沒套用成功」三個小問題，逐一要求 AI 修正 | commit `0991815`、`15012b1`、`d186943`、`c88f154` |

---

## 驗證方式：我要求 AI 用 Chrome 自動化「真的操作」而不是只看程式碼

我在開發過程中要求 AI 大量使用一個 Chrome 瀏覽器自動化工具，讓 AI 可以
像真人一樣點擊網頁、填表單、截圖、看瀏覽器的錯誤訊息，而不是只憑讀
程式碼判斷「應該沒問題」。**這是我對 AI 提出的工作方式要求**，不是 AI
自己決定要這樣做的。

**為什麼我要這樣要求，而不是只寫自動化測試或只靠我自己手動點：**

- 這次踩到的 bug（時區偏移一天、表單送出沒反應、畫面顏色套用錯元素）
  全部都是「瀏覽器實際渲染出來才會出現」的問題，寫程式邏輯測試根本
  測不到，一定要真的在瀏覽器裡跑一次。
- 比起我自己手動點，AI 可以在**我還沒看到之前**先跑過一輪，把明顯的
  問題擋下來，加上它會去讀瀏覽器背後的錯誤訊息跟網路請求，比我肉眼
  掃過去更容易抓到「看起來正常、其實底層出錯」的狀況。

**AI 做過的代表性驗證**（都用假資料測試，測完立刻刪除，並且每次都會
核對真實學生/場地/套組數量沒有被動到——這個測試資料處理原則也是我
要求的）：

- 完整跑過一次「新增套組」的填表單→送出→檢查結果流程，抓到時區跟
  表單送出兩個 bug。
- 完整跑過一次「先開額度、之後在行事曆約時間」的流程：開額度、約課、
  確認扣額度、故意約到超過額度、確認系統擋下來。
- 測試刪除功能：建一個假學生、按刪除、確認跳出的是好看的置中視窗、
  確認刪除後資料真的消失。
- 切換行事曆的週檢視，直接讀取畫面元素「實際套用」的顏色數值，才抓
  到「文字顏色設定了但被別的規則蓋掉」這種肉眼很難分辨的問題。
- 把全部 8 個頁面都重新整理一輪，逐一檢查瀏覽器有沒有跳出任何錯誤
  訊息，確保這麼多次修改下來沒有把其他頁面弄壞。

**這個方法驗不到的部分**（老實承認，面試被問到才不會露餡）：

- 大量排列組合的計算案例（例如各種人數、各種身份別的計價組合）——
  這類案例比較適合寫自動化測試涵蓋。
- 多人同時操作的情況——這個工具是依序點擊測試，沒有模擬「兩人同時搶
  同一筆資料」這種情境；但這套工具本來就是我自己一個人用的記帳工具，
  這個風險本來就低。
- 資料庫層級的完整性（例如刪除學生後，底下的套組資料是不是真的一起
  刪乾淨）——這部分我要求 AI 另外搭配直接呼叫 API、檢查資料庫筆數
  變化來驗證，不是單靠瀏覽器畫面判斷。
